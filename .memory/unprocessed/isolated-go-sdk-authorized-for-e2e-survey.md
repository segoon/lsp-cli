# Isolated Go SDK is authorized for the E2E survey

The user initially chose to record the missing Go SDK as a blocker, then explicitly chose to finish
the manual survey and authorized downloading a pinned official Go SDK into the secure temporary E2E
sandbox. The SDK must not be installed system-wide or added as a Rust dependency.
