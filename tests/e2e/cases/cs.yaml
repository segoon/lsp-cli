language:
  id: cs
  kind: source
  project: playground/csharp

pairs:
  - language: cs
    server: roslyn_ls
    smoke:
      status: excluded
      reason: "The required .NET SDK is unavailable and isolated SDK provisioning was not approved."
    lifecycle:
      status: excluded
      reason: "The required .NET SDK is unavailable and isolated SDK provisioning was not approved."
