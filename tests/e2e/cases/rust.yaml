language:
  id: rust
  kind: source
  project: playground/rust
  query-profile:
    symbol-query: Order
    callable-query: sample_order
    format-file: src/main.rs
    expected-names:
      - Order
      - OrderItem
      - sample_order
      - format_order
pairs:
  - language: rust
    server: rust_analyzer
    smoke:
      status: queries
      exceptions:
        - command: callees
          outcome: empty-matches
          reason: sample_order constructs data directly and has no outgoing call-hierarchy edges
      deadline-seconds: 180
    lifecycle:
      status: scenarios
      direct-run:
        status: exchange
